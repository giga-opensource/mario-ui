## Mario UI

Mario UI is the front end project for Mario(project management tool).

### Install

* Git clone the repo
* Run `npm install`

### Development

* Run `gulp`

### Build Release

* Run `gulp deploy`
