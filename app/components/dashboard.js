var React = require('react');

var Dashboard = React.createClass({
  render: function () {
    return (
      <div>
        <h2>dashboard</h2>
      </div>
    );
  }
});

module.exports = Dashboard;
